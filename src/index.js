/**
 * This is the parent class.
 */
class ParentClass {

    /**
     * This is the static inner class.
     *
     * You can add a name after class, JSDoc will still
     * fail (at the time of writing).
     */
    static InnerClass = class {
        /**
         * @param {number} arg0 - demo argument
         */
        constructor(arg0) {

        }
    }
}
